<Window x:Class="WpfApp2.View.WindowNewStudent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Студент" Height="320" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Labels -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Код:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Имя:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Отчество:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Группа:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Дата рождения:" 
                   VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>

        <!-- Input controls -->
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Id}" IsEnabled="False" 
                 Margin="5" Height="23"/>

        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Height="23"/>

        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Height="23"/>

        <TextBox Grid.Row="3" Grid.Column="1" 
                 Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Height="23"/>

        <!-- ✅ ИСПРАВЛЕНО: x:Name="CbGroupCombo" вместо "CbGroup" -->
        <ComboBox Grid.Row="4" Grid.Column="1" 
                  x:Name="CbGroupCombo"
                  DisplayMemberPath="GroupName" 
                  SelectedValuePath="Id" 
                  SelectedValue="{Binding GroupId}" 
                  Margin="5" Height="23"/>

        <DatePicker Grid.Row="5" Grid.Column="1" 
                    x:Name="DpBirthDate" 
                    SelectedDate="{Binding BirthDate, Mode=TwoWay}" 
                    Margin="5" Height="23"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnSave" Content="Сохранить" Width="80" Height="25" 
                    Margin="5" IsDefault="True" Click="BtnSave_Click"/>
            <Button x:Name="BtnCancel" Content="Отменить" Width="80" Height="25" 
                    Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>